#include "Terrapch.h"

// VS precompiled headers require a source file that includes the pch, where as clang and gcc do not
// To have all includes show up in compiler output: properties (alt+enter) -> advanced -> show includes
